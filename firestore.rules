rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Chỉ tài khoản đã đăng nhập mới được đọc/ghi dữ liệu
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

  }
}
